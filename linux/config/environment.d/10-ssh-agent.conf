# SSH_AUTH_SOCK for everything that is not a shell: user services, and -- via
# the generator start-sway(1) evaluates -- every GUI app in the session.
# %t is unit-file syntax and means nothing here.
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/ssh-agent.socket
