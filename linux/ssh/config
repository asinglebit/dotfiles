# ~/.ssh/config -- linked here by install.sh; OpenSSH reads no XDG path.

Host *
    # Adds a key to the agent on first use, so a passphrase is asked for at
    # most once per login and on the tty of whatever asked for it.
    AddKeysToAgent yes
    IdentityFile ~/.ssh/id_ed25519
